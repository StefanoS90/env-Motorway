# -*- coding: utf-8 -*-

from setuptools import setup

setup(name='gym_Motorway',
      version='0.0.1',
	  packages=setuptools.find_packages(),
      install_requires=['gym','baseconvert']  # And any other dependencies foo needs
)